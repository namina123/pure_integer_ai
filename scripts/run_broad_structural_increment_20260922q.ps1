$ErrorActionPreference='Stop'; $repo='D:\python_projects\zero_ai\pure_integer_ai'; $source=Join-Path $repo 'src'; $database='K:\pure_integer_ai_work\current_runs\discourse-relation-response-graph-20260917b\training.sqlite3'; $courseRoot='K:\pure_integer_ai_work\current_runs\broad-structural-course-20260922q'; $runRoot='K:\pure_integer_ai_work\current_runs\broad-structural-increment-20260922q'; $control='K:\pure_integer_ai_work\orchestration\broad-structural-increment-20260922q-control'; $state=Join-Path $control 'state.json'; $stdout=Join-Path $control 'session.stdout.txt'; $stderr=Join-Path $control 'session.stderr.txt'; New-Item -ItemType Directory -Force -Path $control|Out-Null; function Save-State([hashtable]$v){($v|ConvertTo-Json -Depth 12 -Compress)|Set-Content -LiteralPath $state -Encoding ascii}; try { if(Test-Path $runRoot){throw "run root already exists: $runRoot"}; $course=Join-Path $courseRoot 'development_response_course.int.json'; $manifest=Join-Path $courseRoot 'development_manifest.json'; $parent=Join-Path $courseRoot 'heldout_manifest.json'; foreach($p in @($database,$course,$manifest,$parent)){if(-not(Test-Path $p -PathType Leaf)){throw "missing input: $p"}}; $env:PYTHONPATH=$source; $p=Start-Process python -ArgumentList @('-u','scripts/run_broad_structural_increment.py','--database',$database,'--course',$course,'--manifest',$manifest,'--parent-manifest',$parent,'--run-root',$runRoot) -WorkingDirectory $repo -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru; Save-State @{format='PURE_INTEGER_BROAD_STRUCTURAL_INCREMENT_V1';status='RUNNING';pid=$p.Id;started_utc=[DateTime]::UtcNow.ToString('o');run_root=$runRoot;course_root=$courseRoot;free_dialogue_complete=0}; Wait-Process -Id $p.Id; $receipt=Join-Path $runRoot 'broad_structural_increment_receipt.json'; if(-not(Test-Path $receipt)){throw 'increment receipt missing'}; Copy-Item $receipt (Join-Path $control 'receipt.json') -Force; Save-State @{format='PURE_INTEGER_BROAD_STRUCTURAL_INCREMENT_V1';status='PASS';completed_utc=[DateTime]::UtcNow.ToString('o');run_root=$runRoot;receipt=$receipt;course_root=$courseRoot;free_dialogue_complete=0} } catch {$_|Out-String|Set-Content $stderr -Encoding utf8; Save-State @{format='PURE_INTEGER_BROAD_STRUCTURAL_INCREMENT_V1';status='FAIL';completed_utc=[DateTime]::UtcNow.ToString('o');run_root=$runRoot;course_root=$courseRoot;error=$_.Exception.Message;free_dialogue_complete=0};exit 1}
