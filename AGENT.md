# 项目施工约束

- 禁止把题目、实体、页面、事实、答案、金钥匙或固定回答硬编码到运行时；不得用展示层模板伪造能力提升。
- 所有能力必须经过既有理解、记忆、证据和生成路径；学习模型只能消费公开课程和真实输入，不能绕过来源约束、拒答门或状态合同。
- 学习不足、证据不足、角色/槽位冲突时必须保持原行为或返回 `UNKNOWN`/`CLARIFY`，不得猜测或兜底成确定答案。
- JSONL 仅为公开课程交换载体；训练和大数据运行产物使用 `K:` 盘，D 盘只放工程代码、紧凑文档和源连接。可重建的 K 盘临时数据可删除。
- 数据能力必须使用纯整数、确定性、可跨语言重建的规范记录；SHA、排序、边界和序列化协议也必须可复现。
- 结构体/不可变值优先，避免无必要普通类、宿主状态和隐式全局缓存；新增对象必须有明确的值语义和迁移边界。
- 只做与主线能力有关的专项验证，禁止为小字段重复运行耗时全量测试；未见能力差分必须记录为 `NE`。
- 不读取、修改或重跑旧 private evaluator，不修改论文；不得把 typed 表层、来源检索或设施状态冒充自由理解、广域泛化、长记忆或断奶。
