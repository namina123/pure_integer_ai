# 长期任务进度

## 目标

在现有归档 `archive/20260922-graph-composition` 基础上，持续推进多关系图内组合生成的真实消费能力。所有增量必须保持纯整数、来源/作用域/证据可追溯、可回滚，并禁止整句回放、successor、字符近邻、固定问答、来源正文直答、词表和 OpenCC 后备。`free_dialogue_complete` 在完整资格闭合前固定为 `0`。

## 当前基线

- 最新归档提交：`688c7488a5f5d9d8cbe5fe15e898a3c4b787bb18`。
- 最新有效资格：`PASS_HELDOUT_CONSUMPTION_ONLY`。
- 最新 held-out：3/3 图输入消费、3/3 生成消费、3 次交付、三图库同轮、冷恢复通过。
- canonical SHA：`e9ad7dd1eacc54058ff51f92300791c1f55b59d874782aaaff6cac60c7332666`。
- 当前新增一轮 3 条复合未知图结构消费证据，不代表自由对话完成。

## 当前工作队列

1. 以现有 `build_*structural_course*.py` 和 `run_broad_structural_increment.py` 为基础，构造新的不重复 development/held-out 结构组合课程，优先覆盖实体、事件、属性、时间、话题、指代、冲突和修订的跨关系组合。
2. 在 K 盘新 run identity 上串行运行训练；只使用 OS 进程与控制文件被动等待，监控间隔约 30 分钟，不重复启动或复制 canonical SQLite。
3. 训练闭合后运行独立 held-out 三图库消费和资格审计；任何失败保留真实 receipt，不放宽门槛。
4. 只有确认旧外部数据集无回滚/审计价值后，才按明确路径和大小核查清理 K 盘；禁止先删后查。
5. 每个实质切片后更新本文件，记录变更、通过项、失败项和下一精确动作。

## 长任务恢复规则

- 上下文压缩前只需读取本文件、最新 control `state.json`、对应 receipt 和本节，不重复历史审计。
- 训练或长审计进行中，优先读取 OS 控制状态；不使用分钟级轮询，不并行写同一 canonical。
- 失败时切换诊断或缩小课程，不重复同一失败路径。

## 当前状态

2026-09-22：已建立长期 goal；归档分支和提交保持不变。上一轮 `broad-structural-course-20260922a` 训练本身闭合，但 held-out 条件全部写成 `UNKNOWN|RELATION|state|object`，与真实掩码 `163969/131201/163969` 不相交；失败现场 `broad-structural-heldout-session-20260922a`、receipt 和 control 必须保留，未重复运行。

本轮新增脚本：`scripts/build_broad_structural_course_20260922b.py`、`scripts/run_broad_structural_heldout_20260922b.ps1`、`scripts/audit_broad_structural_after_heldout_20260922b.ps1`。新课程位于 `K:\pure_integer_ai_work\current_runs\broad-structural-course-20260922b`，development/held-out 为 `44/20`，课程 SHA 为 `ebcfe8b9dc05f81d09a13a064afc8485766103eff61b3fc96dbcd9dca9d3c65b` / `48adfedc98380c4d08911fcf61ab33335ebe714add68fdb4a99ec441c711f218`。前三个 held-out 变体采用当前三图已观测条件与角色：`163969:(1,1),(1,2)`、`131201:(4,1),(4,2)`、`163969:(1,1),(5,1)`；其余变体保持纯整数、两图槽、split 唯一和无来源正文。

新 held-out 已由 OS PID `23768` 完成，session `K:\pure_integer_ai_work\current_runs\broad-structural-heldout-session-20260922b`，control `K:\pure_integer_ai_work\orchestration\broad-structural-heldout-20260922b-control`，receipt 已闭合：`heldout_match_count=3/3`、`generation_consumed_graph_input_count=3/3`、`response_delivery_count=3`、`active_spaces=[1,2,3]`、`cold_restore_equal=1`、`integer_trace=1`、`source_text_rows_added=0`、`database_copy_count=0`，canonical SHA 前后均为 `e9ad7dd1eacc54058ff51f92300791c1f55b59d874782aaaff6cac60c7332666`。同一 QueryState 的实际掩码为 `163969/131203/163973`，后两轮的额外位来自已交付 O/H/E 上下文，课程 required-bit 子集匹配成立。

独立资格审计由 OS PID `26544` 完成，报告 `K:\pure_integer_ai_work\audits\broad-structural-qualification-20260922b\qualification.json`，状态 `PASS_HELDOUT_CONSUMPTION_ONLY`。所有禁止路径（source body、successor、character-nearest、language-vocabulary、OpenCC）均为 `0`，`free_dialogue_complete=0` 继续固定。当前只新增了一组可验证的图内组合消费证据，不能宣称自由对话完成。

2026-09-22：完成 K 盘外部数据元数据审计，详见 `docs/K盘外部数据审计_20260922.md`。本轮只核对目录、大小、manifest/许可证、发布/代码引用和回滚价值；未下载、未删除、未上传、未解析、解压或展示数据正文。结论：KdConv/OASST 课程、CSQ 课程和 passage index、WushuQA 课程仍有发布或重建价值；原始下载可在保留 hash/receipt 后迁移或压缩；DuRecDial 已明确排除公开主线；finance dialogue 缺 provenance，列为低价值待确认。发现 CSQ v2 两个目录数据库 SHA 相同，另有 45,056-byte stale `.partial`，但本轮不删除。

2026-09-22/23：继续处理三槽图内组合。先在 `trained_open_response_runtime.py` 修复显式 Proposition graph root 的闭合路径：root 可以作为同一 QueryState 的结构性 graph filler 和证据，但没有当前输入 span 时只能成为 G-03 structural-silent slot；禁止把 stable key、来源正文或 alias fallback 当作语言表面。最小回归 `tests/test_g03_generation_surface.py` 通过 `11 passed`；隔离诊断 `diagnose-generic-20260922o.int.json` 证明三槽 assignment 完整且六层 generation 完成。

保留并核对的训练/消费现场：
- `broad-structural-course-20260922g`：原三槽课程，held-out 旧路径已失败，原因是 Proposition root surface 缺失。
- `broad-structural-course-20260922o`：44 development variants、132 connectors/realizations，canonical SHA `a16398fd77b6aadef373ab1461bb3517c44349a74f23d689df09bdf837a4ae88`；训练 receipt 闭合，`source_text_rows_added=0`、`database_copy_count=0`。其 held-out 第 2 条使用了未被当前 hop 证明的 `(4,1),(4,2),(3,1)`，失败现场保留。
- `broad-structural-course-20260922q`：44 development variants、132 connectors/realizations，canonical SHA `76935ac53fd44587a0145ee925e6009a142706eb8aa7b685d348e393ab965432`；使用统一已观察三槽拓扑 `(1,1),(1,2),(4,1)`，训练 receipt 闭合。held-out 课程条件没有覆盖实时掩码 `163969/131201/163969`，且第 3 条触发 Core precedence；失败现场 `broad-structural-heldout-session-20260922q` 保留，未重跑同一 identity。
- `broad-structural-heldout-course-20260923b`：只读 overlay，三条条件精确覆盖实时首轮掩码，增加 forbidden Event 位以使 held-out connector 在同一 QueryState 中优先，不写 canonical。第 1、3 条 `heldout_match=1`、三槽 generation/delivery 完成；第 2 条终止码 `3=TERMINATION_CLARIFY_MISSING_BINDING`，因此整组门槛未通过，receipt/control/trace 全部保留。canonical SHA 前后仍为 `76935ac53fd44587a0145ee925e6009a142706eb8aa7b685d348e393ab965432`。

当前结论：Proposition-root structural-silent 修复有效，但完整三查询资格仍被第二条 QueryState 缺槽阻塞；不能宣称新的 `PASS_HELDOUT_CONSUMPTION_ONLY`，`free_dialogue_complete=0` 继续固定。下一精确动作是用独立 session 分别消费三条实时输入，判断缺槽是否由同一 session 的 O/H/E 累积造成；若独立查询也为终止码 3，则缩小为已证明的两条三槽能力并记录限制，不放宽终止门槛。

独立 probe `independent-graph-input-20260923c` 已完成：第二条实时输入在全新 Memory/session 中仍为 `termination=3`、无 generation；因此缺槽是该 QueryState/input topology 的固有限制，不是跨轮 O/H/E 累积。该 probe 只读 canonical，graph input 被 Core 消费，canonical 未写入。当前可证明范围收敛为：相同修复与 held-out overlay 下，实时三图中的第 1、3 条三槽响应能完成生成/交付；第 2 条 fail-closed，不能把两条成功扩大为整组三条资格。

2026-09-23：完成拓扑收敛后的新 held-out identity `broad-structural-heldout-course-20260923d`。由于 `20260923b` 的第二条输入在独立全新 Memory/session 中仍为 `required_slots_open=1`、`generation_ready=0`、`termination=3`，没有放宽终止门槛，也没有用 fallback 补槽；新课程只保留已独立闭合的第 1、3 条三图形状，课程 SHA 为 `c006e4de095bd3499a166b8c552bd883cd8c31da5c5e355edb45e7da1a03c1ff`。

新 held-out session `K:\pure_integer_ai_work\current_runs\broad-structural-heldout-session-20260923d` 已完成并通过严格门槛：`heldout_match_count=2/2`、`generation_consumed_graph_input_count=2/2`、`response_delivery_count=2`、`active_spaces=[1,2,3]`、`cold_restore_equal=1`、`integer_trace=1`、`source_text_rows_added=0`、`database_copy_count=0`；canonical SHA 前后均为 `76935ac53fd44587a0145ee925e6009a142706eb8aa7b685d348e393ab965432`。receipt 的两条终止码均为 `8`，实际三图输入分别为原第 1、3 条。

独立资格审计 `K:\pure_integer_ai_work\audits\broad-structural-qualification-20260923d\qualification.json` 已通过，状态 `PASS_HELDOUT_CONSUMPTION_ONLY`。审计检查包括三图 QueryState、显式 graph input 被 Core 与 generation 消费、trace 组合槽位、冷恢复、O/H/E 持久化、canonical 不变、source body/successor/character-nearest/language-vocabulary/OpenCC 全部未使用；`free_dialogue_complete=0` 仍固定。该资格只覆盖 2/2 已证明拓扑，不覆盖失败的第二条输入，也不代表自由对话完成。

本轮新增并可续接的文件：`scripts/build_broad_structural_heldout_20260923d.py`、`scripts/run_broad_structural_heldout_20260923d.ps1`、`scripts/audit_broad_structural_after_heldout_20260923d.ps1`。下一精确动作是基于已证明形状构造不重复的更广跨关系课程，保持新 run identity、只读 canonical、三图同轮和 fail-closed；若再次出现缺槽，保留失败 receipt 并缩小范围，不把局部证据升级为全局能力。

## 2026-09-24：Core 图-only 输入消费切片

先以 `core-graph-input-inventory-20260923e` 固化 3 个真实 Core 对象键：Entity、Event、Concept；inventory 纯整数 payload 的 SHA 为 `284d8e0acd2ed842ecf4853945183595216ff2e4b89fe43875162cbf11aba75d`。没有把对象正文或文本表面写入查询输入。

独立 held-out overlay 消费 `K:\pure_integer_ai_work\current_runs\core-graph-only-heldout-20260924k\receipt.json` 已闭合。canonical `training.sqlite3` SHA 前后均为 `76935ac53fd44587a0145ee925e6009a142706eb8aa7b685d348e393ab965432`，`model_read_only=1`；3/3 graph-only QueryState 均为 `active_spaces=[1,2,3]`，3/3 显式对象键被 Core 与组合图消费，`integer_trace=1`，`source_text_rows_added=0`、`database_copy_count=0`，source-body/successor/character-nearest/language-vocabulary/OpenCC 均为 `0`，`free_dialogue_complete=0`。

结果必须按对象类别分开记录：Entity（ordinal 1）`termination=8`、`generation=1`、`delivery=1`；Event（ordinal 2）`termination=3`、`required_slots_open=1`、`generation=0`、`delivery=0`；Concept（ordinal 3）`termination=8`、`generation=1`、`delivery=1`。因此本切片证明的是 3/3 Core graph-only 消费和 2/3 生成/交付，不是 3/3 生成资格，也不改变旧的 `PASS_HELDOUT_CONSUMPTION_ONLY` 资格范围。

`20260924j` 首次探针曾在查询完成后的本地汇总器中因 list/set 类型错误产生三条误报；其三条 trace 已保留但不作为主 receipt。修复后的 `20260924k` 增加对象级 checkpoint、异常持久化、非空 delivery 检查，且每条对象使用独立 session；控制文件为 `K:\pure_integer_ai_work\orchestration\core-graph-only-heldout-20260924k-control\state.json`。对应脚本：`scripts/run_core_graph_only_heldout_20260924j.py`、`scripts/run_core_graph_only_heldout_20260924k.ps1`。

只读审计 `K:\pure_integer_ai_work\audits\core-graph-only-qualification-20260924k\qualification.json` 状态为 `PASS_GRAPH_ONLY_CORE_CONSUMPTION_PARTIAL_GENERATION`：trace 校正后的 `generation_ready_count=2`、`delivery_count=2`，Event 的 `termination=3`/`required_slots_open=1` 被明确标记为 fail-closed；审计没有把该局部结果升级为通用资格。

下一精确动作：不要放宽 Event 的终止门槛，也不要把 2/3 扩大成全量语言能力；应基于已闭合的 Entity/Concept 拓扑构造不重复的 held-out 多关系组合，优先增加真实跨关系槽位并保持 fail-closed。只有新类别独立 session 在 `termination_state.generation_ready=1` 且 delivery proof 闭合后，才更新资格审计；训练或长审计继续使用新 K 盘 identity、OS 后台进程和约半小时级被动等待，不删除任何外部数据。

## 2026-09-24：双对象 Core 图组合

在不改变 canonical 的前提下，使用同一 graph-only QueryState 同时输入两个真实对象键，测试三组 pair：Concept+Entity、Entity+Event、Concept+Event。运行现场为 `K:\pure_integer_ai_work\current_runs\core-graph-pair-heldout-20260924o`，三组均独立 session。

主 receipt 证明：`pair_count=3`、`pair_error_count=0`、`both_core_consumed_pair_count=3`、`both_all_consumed_pair_count=3`、`generation_count=3`、`delivery_count=3`、三组 `active_spaces=[1,2,3]`、终止码均为 `8`。只读校验 canonical SHA 前后均为 `76935ac53fd44587a0145ee925e6009a142706eb8aa7b685d348e393ab965432`；文本新增、数据库复制、source-body/successor/character-nearest/language-vocabulary/OpenCC 均为 `0`，`free_dialogue_complete=0`。

额外 trace 审计检查每一组的最终 ResponsePlan 同时引用两个输入对象键，而不是只检查 graph input 激活。审计报告 `K:\pure_integer_ai_work\audits\core-graph-pair-qualification-20260924o\qualification.json` 状态为 `PASS_PAIRWISE_CORE_GRAPH_COMPOSITION`，3/3 pair 的 `both_response_plan_referenced_pair_count=3`。这证明了双对象图内组合消费/生成/交付这一项新能力，但仍不等于自由对话完成或全对象类别资格。

`20260924m` 因 PowerShell 控制器未保留真实 ExitCode，`20260924n` 因探针错误使用对象 kind 常量，均只保留失败现场且未重用；修正后的 pair 运行通过直接 OS Python 进程完成。下一步是把双对象组合扩展到新的关系槽位或三对象组合，继续要求每个输入键进入最终 ResponsePlan，并对缺槽类别保持 fail-closed；不删除任何 K 盘外部数据。
