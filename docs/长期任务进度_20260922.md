# 长期任务进度

## 目标

在现有归档 `archive/20260922-graph-composition` 基础上，持续推进多关系图内组合生成的真实消费能力。所有增量必须保持纯整数、来源/作用域/证据可追溯、可回滚，并禁止整句回放、successor、字符近邻、固定问答、来源正文直答、词表和 OpenCC 后备。`free_dialogue_complete` 在完整资格闭合前固定为 `0`。

## 当前基线

- 最新归档提交：`688c7488a5f5d9d8cbe5fe15e898a3c4b787bb18`。
- 最新有效资格：`PASS_HELDOUT_CONSUMPTION_ONLY`。
- 最新 held-out：3/3 图输入消费、3/3 生成消费、3 次交付、三图库同轮、冷恢复通过。
- canonical SHA：`e9ad7dd1eacc54058ff51f92300791c1f55b59d874782aaaff6cac60c7332666`。
- 当前新增一轮 3 条复合未知图结构消费证据，不代表自由对话完成。

## 当前工作队列

1. 以现有 `build_*structural_course*.py` 和 `run_broad_structural_increment.py` 为基础，构造新的不重复 development/held-out 结构组合课程，优先覆盖实体、事件、属性、时间、话题、指代、冲突和修订的跨关系组合。
2. 在 K 盘新 run identity 上串行运行训练；只使用 OS 进程与控制文件被动等待，监控间隔约 30 分钟，不重复启动或复制 canonical SQLite。
3. 训练闭合后运行独立 held-out 三图库消费和资格审计；任何失败保留真实 receipt，不放宽门槛。
4. 只有确认旧外部数据集无回滚/审计价值后，才按明确路径和大小核查清理 K 盘；禁止先删后查。
5. 每个实质切片后更新本文件，记录变更、通过项、失败项和下一精确动作。

## 长任务恢复规则

- 上下文压缩前只需读取本文件、最新 control `state.json`、对应 receipt 和本节，不重复历史审计。
- 训练或长审计进行中，优先读取 OS 控制状态；不使用分钟级轮询，不并行写同一 canonical。
- 失败时切换诊断或缩小课程，不重复同一失败路径。

## 当前状态

2026-09-22：已建立长期 goal；归档分支和提交保持不变。上一轮 `broad-structural-course-20260922a` 训练本身闭合，但 held-out 条件全部写成 `UNKNOWN|RELATION|state|object`，与真实掩码 `163969/131201/163969` 不相交；失败现场 `broad-structural-heldout-session-20260922a`、receipt 和 control 必须保留，未重复运行。

本轮新增脚本：`scripts/build_broad_structural_course_20260922b.py`、`scripts/run_broad_structural_heldout_20260922b.ps1`、`scripts/audit_broad_structural_after_heldout_20260922b.ps1`。新课程位于 `K:\pure_integer_ai_work\current_runs\broad-structural-course-20260922b`，development/held-out 为 `44/20`，课程 SHA 为 `ebcfe8b9dc05f81d09a13a064afc8485766103eff61b3fc96dbcd9dca9d3c65b` / `48adfedc98380c4d08911fcf61ab33335ebe714add68fdb4a99ec441c711f218`。前三个 held-out 变体采用当前三图已观测条件与角色：`163969:(1,1),(1,2)`、`131201:(4,1),(4,2)`、`163969:(1,1),(5,1)`；其余变体保持纯整数、两图槽、split 唯一和无来源正文。

新 held-out 已由 OS PID `23768` 完成，session `K:\pure_integer_ai_work\current_runs\broad-structural-heldout-session-20260922b`，control `K:\pure_integer_ai_work\orchestration\broad-structural-heldout-20260922b-control`，receipt 已闭合：`heldout_match_count=3/3`、`generation_consumed_graph_input_count=3/3`、`response_delivery_count=3`、`active_spaces=[1,2,3]`、`cold_restore_equal=1`、`integer_trace=1`、`source_text_rows_added=0`、`database_copy_count=0`，canonical SHA 前后均为 `e9ad7dd1eacc54058ff51f92300791c1f55b59d874782aaaff6cac60c7332666`。同一 QueryState 的实际掩码为 `163969/131203/163973`，后两轮的额外位来自已交付 O/H/E 上下文，课程 required-bit 子集匹配成立。

独立资格审计由 OS PID `26544` 完成，报告 `K:\pure_integer_ai_work\audits\broad-structural-qualification-20260922b\qualification.json`，状态 `PASS_HELDOUT_CONSUMPTION_ONLY`。所有禁止路径（source body、successor、character-nearest、language-vocabulary、OpenCC）均为 `0`，`free_dialogue_complete=0` 继续固定。当前只新增了一组可验证的图内组合消费证据，不能宣称自由对话完成。

2026-09-22：完成 K 盘外部数据元数据审计，详见 `docs/K盘外部数据审计_20260922.md`。本轮只核对目录、大小、manifest/许可证、发布/代码引用和回滚价值；未下载、未删除、未上传、未解析、解压或展示数据正文。结论：KdConv/OASST 课程、CSQ 课程和 passage index、WushuQA 课程仍有发布或重建价值；原始下载可在保留 hash/receipt 后迁移或压缩；DuRecDial 已明确排除公开主线；finance dialogue 缺 provenance，列为低价值待确认。发现 CSQ v2 两个目录数据库 SHA 相同，另有 45,056-byte stale `.partial`，但本轮不删除。

下一精确动作：建立仅含 manifest、许可证、源 URL/DOI、版本、hash、转换参数和 receipt 的 K 盘归档索引；随后在零运行依赖且可恢复归档成立后，优先处理 stale partial 和已确认重复的 CSQ v2 目录。删除前仍需再次检查控制文件、发布 manifest 和进程；之后继续以真实 QueryState 掩码和跨关系角色构造下一组独立 development/held-out 课程。
